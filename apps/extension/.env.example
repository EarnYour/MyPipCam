# Copy to `.env.local` (gitignored) for local builds, then rebuild:
#   cp .env.example .env.local && npm run build
# Chrome-extension OAuth *client ID* only — never a client_secret.
#
# Prefer two Google Cloud clients (type: Chrome extension):
#   Client A (store zip only): Item ID meiehjfjcaahfjcdneoegjkmajbfghmm
#   Client B (this file):      Item ID akpchobfndfddajiihkkdpnihihdicjc
# Put Client B here for daily local Drive. Do not commit real IDs.
# See AGENTS.md, README.md § Google Drive, docs/marketing/CHROME_WEBSTORE.md.

VITE_GOOGLE_OAUTH_CLIENT_ID=YOUR_CLIENT_ID.apps.googleusercontent.com
